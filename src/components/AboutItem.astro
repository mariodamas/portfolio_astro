---
const { title, image_url } = Astro.props;
---
<div class="flex flex-col items-center text-center">
    <img src={image_url} alt={title} class="w-full h-auto mb-4 rounded-lg" />
    <div>
        <h5 class="mb-2 text-s font-semibold tracking-tight text-gray-900 dark:text-white">{title}</h5>
    </div>
</div>