---
const { date, title, school, school_logo } = Astro.props;
---
<div class="flex flex-col items-center text-center">
    <img src={school_logo} alt={school} class="w- h-20 mb-4" />
    <div>
        <h5 class="mb-2 text-2xl font-semibold tracking-tight text-gray-900 text-white">{title}</h5>
    </div>
    <p class="mb-3 font-normal text-gray-500 text-gray-400">{school}</p>
    <p class="inline-flex font-medium items-center text-blue-600">
        {date}
    </p>
</div>